# 🧠 Example environment configuration for ScenarioCase Scribe
# Copy this file as `.env` and update with your real credentials.
# Never commit your actual `.env` file — it is ignored in .gitignore.

# n8n Basic Auth credentials (for securing your instance)
N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=<your-username>
N8N_BASIC_AUTH_PASSWORD=<your-password>

# AI Provider (e.g., OpenAI)
OPENAI_API_KEY=<your-api-key>

# Webhook base URL (used for form submission)
WEBHOOK_BASE_URL=https://scribe.qaiagentslab.cloud

# Server & Port configuration
N8N_PORT=5678
N8N_PROTOCOL=http

# Execution / data settings
EXECUTIONS_PROCESS=main
GENERIC_TIMEZONE=Asia/Kolkata
